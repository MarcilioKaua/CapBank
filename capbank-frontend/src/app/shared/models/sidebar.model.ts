export interface QuickAction {
  id: string;
  title: string;
  subtitle: string;
  icon: string;
  color: string;
  route: string;
}

export interface MenuItem {
  id: string;
  label: string;
  icon: string;
  route: string;
}